/*
val arra = Array(3,10,2,1,20)
val sizeOfSubSequence = Array(1,1,1,1,1)

(i to n) foreach { k =>
(0 to k) foreach { m =>
     if (arr[k] > arr[m]) sizeOfSubSequence[k] =  max( sizeOfSubSequence[k], 1 + sizeOfSubSequence[m])
}

}


 */