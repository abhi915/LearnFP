import scala.annotation.tailrec

/*

 compare with adjacent element and largest element place at rightmost side.
----
bubble sort bit complex and inefficient with immutable list...We swap adjacent element
 */

def bubbleSort(data: List[Int]) = {
   for(i <- 0 to data.size) {
     for(j <- 1 until  data.size-i) {

     }
   }
}




